---

- name: Install nginx
  become: yes
  apt: 
    name: nginx 
    state: present 
    update_cache: true


#- name: Removing default nginx config
 # include_vars:
  #  file: 
   #   name=/etc/nginx/sites-enabled/default state=absent


- name: Install system nginx config
  template:
    src: templates/nginx.conf
    dest: /etc/nginx/nginx.conf
