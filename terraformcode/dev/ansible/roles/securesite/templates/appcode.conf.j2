server {
  listen 443 ssl;
  server_name localhost;

  root /var/www/html/appcode;
  index index.php index.html index.htm;
  ssl_certificate /etc/nginx/ssl/server.crt;
  ssl_certificate_key /etc/nginx/ssl/server.key;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
